(defcfg
  process-unmapped-keys yes
)

(defsrc
  caps lctl lmet q
)

(deflayer default
  lctl caps lmet q
)

;; Block the Logitech MX Keys Mini lock button macro (lctl+lmet+q).
(defoverrides
  (lctl lmet q) (nop0)
)
