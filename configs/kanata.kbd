(defcfg
  ;; Helps the OS ignore the physical state of the keys 
  ;; and listen only to Kanata's virtual events.
  process-unmapped-keys yes
)

(defsrc
  caps lctl
)

(deflayer default
  lctl caps
)
